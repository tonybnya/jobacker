[project]
name = "JobPipeline"
version = "0.1.0"
description = "Job Application Tracker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "backend",
]
